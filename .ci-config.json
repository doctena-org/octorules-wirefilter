{
  "_comment": "Single source of truth for the octorules family's CI matrix. Provider repos fetch this from a release tag, never from main: a bad commit here must not break every repo's CI at once. Each repo also keeps a checked-in fallback copy, because octodns's equivalent has no --fail, no retry and no fallback, so a raw.githubusercontent blip blocks every PR in every provider repo. Keys live here only once a workflow reads them.",
  "python_versions_active": [
    "3.10",
    "3.11",
    "3.12",
    "3.13",
    "3.14"
  ]
}
